import { ArgumentsValidationPipe } from './arguments-validation.pipe';

describe('ArgumentsValidationPipe', () => {
  it('should be defined', () => {
    expect(new ArgumentsValidationPipe()).toBeDefined();
  });
});
